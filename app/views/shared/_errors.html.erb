<% if obj.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <!-- la siguiente línea hace que si hay más de un error diga que hay errors y no error-->
    <h3>
    <%= pluralize(obj.errors.count, "error") %>
    no permiten crear el perfil
    </h3>
    <div class="panel-body">
      <ul>
        <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
